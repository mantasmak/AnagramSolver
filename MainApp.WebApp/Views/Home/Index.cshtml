@model  MainApp.WebApp.Models.WordViewModel
@{
    ViewData["Title"] = "Home Page";
}

<h2>@Model.Name</h2>

<ul>
    @foreach (var word in Model.Anagrams)
    {
        <li>
            @word
        </li>
    }
</ul>